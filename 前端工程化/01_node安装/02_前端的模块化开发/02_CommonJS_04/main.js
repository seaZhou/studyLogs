// 结论一:模块在被第一次引入时,模块中的代码会被运行一次
console.log('---------------')
const foo = require('./foo')
console.log('+++++++++++++++++')

// 结论二:模块被多以引入时,会缓存,最终只加载(运行)一次
const foo2 = require('./foo')

// 如果有循环引入,那么加载顺序是什么? :深度优先算法(图结构)
// 数组结构/链表结构/树结构